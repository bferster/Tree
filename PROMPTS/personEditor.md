**PERSON EDITOR**


	This module provides controls in the Verité user app for setting the search criteria for searching for matches in the source data to a list of attributes for a person.

	- Intuitive Layout: Grouped fields for clear input organization.
	- Modern Aesthetics: Uses neutral tones, subtle shadows, and rounded corners to fit a premium "Verité" design language.
	- Responsive Design: Adapts smoothly from mobile touch screens to desktop environments

	Add a control that facilitates the editing of fields. It is a combination of a select menu with the option to add an entry manually.
- The option is encased in a rounded corner box whole fill color can be specified on a line by line basis.
- Extend the pill for the entire space provided for VALUE and enclose the options within the pill.
- VALUE can only have one element

This fields in the person object are as follows:

	person_id
	first_name
	norm_first_name
	last_name
	NYSIIS_last_name
	soundex
	suffix
	race
	gender
	birth_year
	death_year
	mentions 
	confidence


- Mentions and person_id  are not listed as a factor.
- norm_first_name is labeled “Nick name” in factors list


**Value field control**

The value field has a control that combines a pill encased values display, an option manual.

text-entry control and a pulldown selection menu.

The options are:
value: the display text shown in pill
option: the text shown in the pull down
adding a value using the keyboard.

Each field had its own options:

first_name:	 [ “John”, “ALB-CN1880”]
  “Jack”, “ALB-CN1870”]
norm_first_name “ JACK”
last_name:	 [ “Johnson”, “ALB-CN1870”]
			 [ “Johnson”, “ALB-CN1880”]
NYSIIS: JHNSN
soundex: “DF5”
suffix (Jr/Sr)
race (B/W)
gender (M/F)
birth_year:	 [ “1832”, “ALB-CN1870”]
			 [ “1832”, “ALB-CN1880”]
linked_persons: [ “Al Johnson : Son”, “ALB-CN1880”]
			  [ “Mary Johnson : Wife”, “ALB-CN1880”]
			  [ “Ralph Fox : Father”, “ALB-CN1880”]
death_year

Factor options:

Soundex, norm_first_name and NYSIIS factors are generated by the app and are not editable.
You cannot manually edit gender, race, or suffix., but you can select from their options.
Add an option to all editable value controls to add a blank value 
The blank option should not be added to the options list and it's value should be null . The add blank option should be labeled "Make blank".
List  all relationships in the linked_persons field separately as options and simply show the number of links in the pill. Don’t allow selection of linked people. Always show all of them. Instead of 3 links in pill, say 3 linked people. Don’t draw IMPACT stars or COMPARE options for linked_persons.




The factors appear in a section labeled “FACTORS”.
Each line has following information:
FACTOR: lists a human readable name of the factor
VALUE: is a control that can show multiple options, or write in a value
IMPACT: show the impact weight of the factor, from 1 stat to 4 stars in traditional  rating  control interactivity. The stars should appear like rating stars, representing a value from 1-4. A 1 should have the 1st filled in using yellow, 2 the first 2, etc.
COMPARE: offers a number of pill encased options  that are used for comparison.   It can contain multiple values for some factors
The first row is a header row: FACTOR, VALUE, IMPACT, COMPARE 
The options are listed horizontally and clicking on one toggles them on and off
All  have an “ignore” pill at start which turns off all the other pills in the row
first_name, last_name, norm_first_name, and NYSIIS have “exact”, “fuzzy”, and “rarity” options added.
NYSIIS and Soundex have “exact” and “rarity” options added.
suffix, race, and gender factor have an “exact” option added.
birth_year and death_year have “exact”, “±  1”, “±  2”, “±  3”, and “±  5”. The options should act as radio buttons. Clicking one should turn off all the others.
Norm_first_name 

SEARCH button in footer should be filled, have a magnifier icon and be labeled in uppercase.

Add a pulldown menu to the left of the SEARCH button, labeled “Sources” that contains a list of the sources to search on. Multiple sources can be set and there is a button to toggle all the selections on or off.

Add a 4 star rating indicator on the left side of the footer, labeled “ Verity:”. This is set by the app, not the user. It should resemble the stars in the factor‘s IMPACT display.

*Delivery*
Factor as a plain vanilla JavaScript object that can use jquery and jqueryui. It can be embedded into a tab’s div and called by ShowPersonEditor(personId).
PersonID is this object:

	{ 
	person_id,
	first_name,
	norm_first_name,
	last_name,
	nysiis_last_name,
	soundex_last_name,
	suffix,
	race,
	gender,
	birth_year,
	death_year,
	confidence	
	mentions[],	
	linked_persons[]
	}

when a first_name is set in PERSON EDITOR, convert that value into a nickname using the algorithm outlined in 

Normalize.md
 and place as value in the Nick Name factor VALUE control
when a last_name is set in PERSON EDITOR, convert that value into a NYSSIS value using the algorithm outlined in 

Normalize.md
 and place as value in the NYSIIS factor VALUE control
when a last_name is set in PERSON EDITOR, convert that value into a Soundex value using the algorithm outlined in 

Normalize.md
 and place as value in the Soundec factor VALUE control

